# Transform-or-Rotate-Nav


$ git clone https://github.com/alwaysappear/Transform-or-Rotate-Nav.git

$ cd Transform-or-Rotate-Nav

$ npm install

$ npm run watch


Go to ./public and run index.html as Live Server on VS code.
